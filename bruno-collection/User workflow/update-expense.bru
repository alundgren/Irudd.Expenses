meta {
  name: update-expense
  type: http
  seq: 5
}

post {
  url: {{host}}/v1/expenses/update
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "expenseId": "{{latestAddedExpenseId}}",
    "amount": 400,
    "categoryCode": "other"
  }
}
